import React from 'react'

const Home = ()=>{
    return(
        <div className="container">
        <h2>Home</h2>
        <p>This is a simple website where i display posts. This website was made with a purpose to learn react with react router and axios for handling HTTP requests.</p>
        <p>In this website, i fetch data provided by JSONPLACEHOLDER using axios GET method and display them.</p>
        <p>This website follows a react with redux tutorial playlist by the net ninja.</p>
        </div>
    )
}

export default Home